import {GeneratorFunction, Level} from "./transformer"

export const timestamp2Day: GeneratorFunction = (propoName: string, rows: any, prevLevel?: Level): Level=>{
    return
}

export const timestamp2Week: GeneratorFunction = (propoName: string, rows: any, prevLevel?: Level): Level=>{
    return
}
